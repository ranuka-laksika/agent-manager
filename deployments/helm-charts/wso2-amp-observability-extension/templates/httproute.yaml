{{- if and .Values.amObserver.enabled .Values.amObserver.ocIngress.enabled }}
# HTTPRoute exposing the Agent Manager Observer through the observability-plane
# kgateway (gateway-default). This chart installs into the same namespace as
# that gateway, so no ReferenceGrant is needed.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Release.Name }}-traces
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: amp-observer
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  hostnames:
    - {{ .Values.amObserver.ocIngress.hostname }}
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .Values.amObserver.ocIngress.gatewayName }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      timeouts:
        request: {{ .Values.amObserver.ocIngress.requestTimeout | default "60s" }}
      backendRefs:
        - group: ""
          kind: Service
          name: {{ .Values.amObserver.name }}
          port: {{ .Values.amObserver.port }}
          weight: 1
{{- end }}
